version = 1

[merge]
min_approvals = 1
required_checks = []
allow_author_approval = false
strategy = "fast-forward"

# Agents use the same proposal protocol as people, with capabilities granted
# independently. Principals and check names must be valid Git ref components.
[[agents]]
principal = "svc-ci"
checks = ["tests"]
review = false
merge = false
